# Unset VITE_API_BASE_URL (or omit this file) to run against in-memory mock data
# with no sign-in. Set it to talk to LifeOs.Api — then Google auth is required.

VITE_API_BASE_URL=http://localhost:5280
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
