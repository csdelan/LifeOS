namespace LifeOs.Application;

/// <summary>
/// Anchors the <c>LifeOs.Application</c> assembly. Transport-neutral application
/// services (capture, subject creation, linking, promotion, status transitions)
/// arrive with the M2/M3 issues. This type exists only so the project has a
/// stable namespace to build against and can be removed once real services land.
/// </summary>
internal static class PlaceholderMarker;
