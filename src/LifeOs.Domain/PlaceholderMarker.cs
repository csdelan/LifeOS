namespace LifeOs.Domain;

/// <summary>
/// Anchors the <c>LifeOs.Domain</c> assembly. The Stage 1 domain model — events,
/// artifacts, subjects, relations, and the rules over them — is introduced by
/// later M1 issues (#3–#5). This type exists only so the project has a stable
/// namespace to build against and can be removed once real types land.
/// </summary>
internal static class PlaceholderMarker;
